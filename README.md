# Installation

In top-most dir
```bash
$ virtualenv venv
$ . venv/bin/activate
$ pip install flask-restful
$ pip install Flask-SQLAlchemy
$ pip install -U marshmallow
```

# Run server
```bash
$ python runserver.py
```
